%table.table
  %thead
    %tr
      %th Username
      %th E-mail
      %th.last &nbsp;
    %tr
  - users.each do |user|
    %tr{:class=> cycle("odd","even", :name=>"row_class"), :id=>"user"+user.id.to_s} 
      %td #{user.username}
      %td #{mail_to user.email}
      -if modifiable
        %td.last
          <a href="#"> show </a> <a href="#"> edit </a> 
          #{link_to "delete", [:admin, user], :method=>"delete", :remote=> true, :confirm=>"Delete this user?"}
      
 